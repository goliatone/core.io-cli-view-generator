<!--
Autogenerated file: {{label}}
{{now|date('Y-m-d h:m:s')}}
-->
<div id="form-content" class="content-table">
    <table id="{{identity}}_grid" class="display" cellspacing="0" width="100%">
        <thead>
            <tr>{% for field in fields %}
                <th>{{field.label}}</th>{% endfor %}
            </tr>
        </thead>
        <tfoot>
            <tr>{% for field in fields %}
                <th>{{field.label}}</th>{% endfor %}
            </tr>
        </tfoot>

        <tbody>
        <%
            records && records.forEach(function(item){ %>
                <tr>
                {%- for field in fields %}
                    <td>
                        <%= item.{{field.name}} %>
                    </td>
                {%- endfor %}
                </tr>
        <% }) %>
        </tbody>
    </table>
</div>
