'use strict';

function postRelationshiptTypes(schema) {
    //Figure out how many relTypes we have.
    //If is object then find the other and
    //ensure they have the right type.
    
    return schema;
}

module.exports = postRelationshiptTypes;
